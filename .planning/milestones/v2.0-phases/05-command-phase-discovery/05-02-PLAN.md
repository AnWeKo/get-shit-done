---
phase: 05-command-phase-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - commands/gsd/plan-all.md
  - get-shit-done/workflows/plan-all.md
autonomous: true
requirements:
  - CMD-01
  - CMD-02
  - CMD-03

must_haves:
  truths:
    - "User can type /gsd-plan-all and the command is recognized in Claude Code, OpenCode, and Gemini CLI"
    - "The command file has valid YAML frontmatter with name, description, argument-hint, allowed-tools"
    - "The workflow calls gsd-tools.cjs init plan-all and roadmap unplanned for discovery"
    - "When unplanned phases exist, the workflow displays a summary table with phase number, name, and disk status"
    - "When no unplanned phases exist, the user sees a clear nothing-to-plan message with next-step suggestions"
    - "The --dry-run flag shows discovery output and exits without planning"
  artifacts:
    - path: "commands/gsd/plan-all.md"
      provides: "Slash command entry point for /gsd-plan-all"
      contains: "gsd:plan-all"
    - path: "get-shit-done/workflows/plan-all.md"
      provides: "Discovery workflow with phase identification logic"
      min_lines: 80
  key_links:
    - from: "commands/gsd/plan-all.md"
      to: "get-shit-done/workflows/plan-all.md"
      via: "execution_context @-reference"
      pattern: "workflows/plan-all"
    - from: "get-shit-done/workflows/plan-all.md"
      to: "gsd-tools.cjs init plan-all"
      via: "CLI call for context initialization"
      pattern: "gsd-tools.cjs init plan-all"
    - from: "get-shit-done/workflows/plan-all.md"
      to: "gsd-tools.cjs roadmap unplanned"
      via: "CLI call for phase discovery"
      pattern: "gsd-tools.cjs roadmap unplanned"
---

<objective>
Create the `/gsd-plan-all` slash command and its discovery workflow that identifies unplanned phases from ROADMAP.md.

Purpose: This is the user-facing entry point for batch phase planning. The workflow handles discovery (which phases need planning?) and reporting (summary table + status). Phase 6 extends this workflow to add the sequential planning loop. This plan delivers a fully functional command that discovers and reports — but does not yet plan.

Output: A working `/gsd-plan-all` command that shows which phases need planning, with `--dry-run` support, compatible across all three runtimes.
</objective>

<execution_context>
@/home/andreas/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/andreas/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-command-phase-discovery/05-CONTEXT.md
@.planning/phases/05-command-phase-discovery/05-01-SUMMARY.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md

Key reference files (read these for pattern copying):
@commands/gsd/plan-phase.md (command pattern — follow this exact structure)
@get-shit-done/workflows/plan-phase.md (workflow pattern — similar orchestration structure)
@get-shit-done/references/ui-brand.md (banner and table formatting patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create slash command file</name>
  <files>commands/gsd/plan-all.md</files>
  <action>
Create `commands/gsd/plan-all.md` following the exact pattern from `commands/gsd/plan-phase.md`.

**Frontmatter (Claude Code canonical format):**
```yaml
---
name: gsd:plan-all
description: Batch-plan all unplanned phases in the current roadmap with zero interaction
argument-hint: "[--dry-run]"
agent: gsd-planner
allowed-tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
  - Task
  - WebFetch
  - mcp__context7__*
---
```

**Body structure:**
```xml
<objective>
Batch-plan all unplanned roadmap phases. Discovers which phases need planning,
then plans each one sequentially using the existing plan-phase pipeline.

**Default flow:** Discover unplanned phases → Plan each sequentially → Report

**Orchestrator role:** Initialize, discover unplanned phases, display summary,
plan each phase in order (Phase 6 adds the loop), report results.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/plan-all.md
@~/.claude/get-shit-done/references/ui-brand.md
</execution_context>

<context>
Flags:
- `--dry-run` — Show discovery results and exit without planning

$ARGUMENTS passed through to workflow.
</context>

<process>
Execute the plan-all workflow from @~/.claude/get-shit-done/workflows/plan-all.md end-to-end.
Preserve all workflow gates (validation, discovery, reporting).
</process>
```

**Critical conventions (from CONVENTIONS.md / ARCHITECTURE.md):**
- `name` uses `gsd:plan-all` (colon separator — Claude Code format, installer converts for other runtimes)
- `agent` references `gsd-planner` (the existing planner agent)
- `allowed-tools` includes same set as plan-phase (Read, Write, Bash, Glob, Grep, Task, WebFetch, mcp__context7__*)
- No `question` tool needed — zero interaction per requirements
- Body uses XML tags matching plan-phase.md structure exactly
  </action>
  <verify>
    <automated>node get-shit-done/bin/gsd-tools.cjs frontmatter get commands/gsd/plan-all.md 2>&1 | head -10</automated>
    <manual>Verify file has valid YAML frontmatter with name, description, argument-hint, allowed-tools fields. Body references workflow file via @-path.</manual>
  </verify>
  <done>
Command file exists at `commands/gsd/plan-all.md` with valid frontmatter following exact GSD conventions. Body references `plan-all.md` workflow. Compatible with installer cross-compilation (Claude Code canonical format → OpenCode/Gemini).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create discovery workflow</name>
  <files>get-shit-done/workflows/plan-all.md</files>
  <action>
Create `get-shit-done/workflows/plan-all.md` following the step-based `<process>` structure of `plan-phase.md`.

**Structure:**

```xml
<purpose>
Batch-plan all unplanned roadmap phases. Discovers which phases need planning, displays
a summary table, then plans each sequentially. This file handles discovery and reporting —
Phase 6 extends it to add the sequential planning loop.
</purpose>

<required_reading>
Read all files referenced by the invoking prompt's execution_context before starting.

@~/.claude/get-shit-done/references/ui-brand.md
</required_reading>

<process>

## 1. Initialize

```bash
INIT=$(node ~/.claude/get-shit-done/bin/gsd-tools.cjs init plan-all)
```

Parse JSON for: `research_enabled`, `plan_checker_enabled`, `commit_docs`, `planning_exists`, `roadmap_exists`, `unplanned_phases`, `unplanned_count`, `total_phases`, `state_path`, `roadmap_path`, `requirements_path`.

**If `planning_exists` is false:** Error — run `/gsd:new-project` first.
**If `roadmap_exists` is false:** Error — no ROADMAP.md found.

## 2. Parse Arguments

Extract from $ARGUMENTS:
- `--dry-run` flag

## 3. Discover Unplanned Phases

Display banner:
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 GSD ► DISCOVERING UNPLANNED PHASES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

Use `unplanned_phases` from init JSON (already computed).

**If `unplanned_count` is 0 (nothing to plan):**

Display:
```
All {total_phases} phases have plans.

Nothing to plan. Your project is fully planned!

───────────────────────────────────────────────────────────────

## ▶ Next Up

**Execute your plans** — start running the phase plans

`/gsd:execute-phase {next_executable_phase}`

<sub>`/clear` first → fresh context window</sub>

───────────────────────────────────────────────────────────────

**Also available:**
- `/gsd:progress` — view project progress
- `/gsd:verify-work {N}` — verify a completed phase

───────────────────────────────────────────────────────────────
```

Exit workflow.

**If unplanned phases found:**

Display discovery table:
```
Found {N} phase(s) to plan:

| Phase | Name | Status |
|-------|------|--------|
| 5 | Command & Phase Discovery | discussed |
| 6 | Sequential Planning Loop | empty |
| 7 | Retry & Completion | empty |
```

Status column maps disk_status values:
- `no_directory` → "empty"
- `empty` → "empty"
- `discussed` → "discussed"
- `researched` → "researched"

## 4. Dry Run Exit

**If `--dry-run` flag:**

Display: `Dry run — exiting without planning.`

Exit workflow.

## 5. Planning Loop Placeholder

<!-- Phase 6 adds the sequential planning loop here.
     For now, display what Phase 6 will do: -->

Display:
```
Discovery complete. Sequential planning not yet implemented.

To plan individual phases:
/gsd:plan-phase 5
/gsd:plan-phase 6
/gsd:plan-phase 7
```

This placeholder gets replaced by Phase 6's sequential loop implementation.

</process>

<offer_next>
Output this markdown directly (not as a code block):

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 GSD ► DISCOVERY COMPLETE ✓
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Found {N} phase(s) to plan.

───────────────────────────────────────────────────────────────

## ▶ Next Up

**Plan individual phases** — or wait for Phase 6 (sequential loop)

`/gsd:plan-phase {first_unplanned}`

<sub>`/clear` first → fresh context window</sub>

───────────────────────────────────────────────────────────────
</offer_next>

<success_criteria>
- [ ] .planning/ directory validated
- [ ] ROADMAP.md exists and parsed
- [ ] Unplanned phases correctly identified
- [ ] Summary table displayed with phase number, name, status
- [ ] --dry-run flag exits after discovery without planning
- [ ] Nothing-to-plan case shows clean message with next steps
- [ ] User sees discovery results before any planning begins
</success_criteria>
```

**CRITICAL patterns from CONTEXT.md (locked decisions):**
- "Unplanned" = NO PLAN.md files AND not marked complete (double-safe filtering)
- Discovery output is a summary table: phase number, name, disk status
- Include count line: "Found N phases to plan"
- "Nothing to plan" message: clean status + suggested next steps
- No confirmation gate before planning starts (zero interaction)
- Follow existing GSD banner style (ui-brand.md patterns)
- Single flag: `--dry-run` only (no --from, no milestone filter, no pipeline overrides)

**CRITICAL: Workflow extensibility for Phase 6:**
- Step 5 is a clear placeholder that Phase 6 replaces with the actual sequential planning loop
- The workflow structure (init → discover → display → plan) must allow Phase 6 to replace Step 5 without restructuring Steps 1-4
- Mark Step 5 with a comment like `<!-- Phase 6 replaces this section -->`

**Installer compatibility (CMD-03):**
After creating both files, verify the command file works with the installer by checking the command follows Claude Code canonical format (the installer converts automatically during `npx get-shit-done-cc`). The key conventions are:
- `@~/.claude/` paths in the body (installer rewrites these per runtime)
- `gsd:` prefix on command name (installer strips colon for OpenCode/Gemini)
- `allowed-tools` list (installer maps tool names per runtime)
- No `AskUserQuestion` tool (this command has zero interaction)

No need to run the installer — just verify the command file structure matches existing commands like `plan-phase.md` which already work across runtimes.
  </action>
  <verify>
    <automated>node get-shit-done/bin/gsd-tools.cjs frontmatter get commands/gsd/plan-all.md 2>&1 | head -10</automated>
    <manual>Verify workflow file has Steps 1-5, calls init plan-all, displays discovery table, handles dry-run and nothing-to-plan cases. Verify command file matches plan-phase.md structure.</manual>
  </verify>
  <done>
Command file exists with valid frontmatter matching GSD conventions. Workflow file has complete discovery logic (init, parse, discover, display table, dry-run exit). Nothing-to-plan case shows clean message. Step 5 has clear placeholder for Phase 6 extension. Both files follow installer-compatible canonical format.
  </done>
</task>

</tasks>

<verification>
1. `commands/gsd/plan-all.md` has valid YAML frontmatter with all required fields
2. `get-shit-done/workflows/plan-all.md` has `<purpose>`, `<process>` with Steps 1-5, `<offer_next>`, `<success_criteria>`
3. Workflow calls `gsd-tools.cjs init plan-all` in Step 1
4. Workflow uses `unplanned_phases` from init JSON for discovery
5. Discovery table shows phase number, name, and disk status
6. `--dry-run` flag exits after displaying discovery table
7. "Nothing to plan" case has clean message with suggested next steps
8. Step 5 has clear placeholder comment for Phase 6 extension
9. Command file follows exact same structure as plan-phase.md (installer compatibility)
10. No `AskUserQuestion` references (zero interaction requirement)
</verification>

<success_criteria>
- Command recognized when installed (follows canonical Claude Code format)
- Workflow correctly discovers unplanned phases using gsd-tools primitives from Plan 01
- Discovery table is formatted consistently with GSD ui-brand patterns
- --dry-run shows discovery and exits cleanly
- Nothing-to-plan produces helpful next-step suggestions
- Workflow structure allows Phase 6 to extend without restructuring
- No interactive prompts — fully autonomous discovery
</success_criteria>

<output>
After completion, create `.planning/phases/05-command-phase-discovery/05-02-SUMMARY.md`
</output>
