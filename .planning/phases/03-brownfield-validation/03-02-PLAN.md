---
phase: 03-brownfield-validation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - get-shit-done/workflows/new-project-from-spec.md
autonomous: true

must_haves:
  truths:
    - "After research completes, spec assumptions are compared against research findings"
    - "Major contradictions (high impact + high confidence) pause the pipeline for user input"
    - "Minor contradictions are noted as flags in PROJECT.md Key Decisions table without blocking"
    - "User can resolve major contradictions by choosing: Keep spec / Accept research / Custom answer"
    - "Custom resolutions update PROJECT.md and flow through to requirements"
    - "Resolved contradictions ripple into REQUIREMENTS.md when they change a requirement"
  artifacts:
    - path: "get-shit-done/workflows/new-project-from-spec.md"
      provides: "Step 10.5: Spec-vs-research validation with contradiction surfacing"
      contains: "contradiction"
  key_links:
    - from: "Step 10 (research completion)"
      to: "Step 10.5 (validation)"
      via: "research findings compared against spec assumptions"
      pattern: "research.*contradiction\\|contradiction.*research"
    - from: "Step 10.5 (resolved contradictions)"
      to: "Step 7 (PROJECT.md updates)"
      via: "custom resolutions update Key Decisions table"
      pattern: "Key Decisions"
    - from: "Step 10.5 (contradiction resolutions)"
      to: "Step 11 (REQUIREMENTS.md)"
      via: "resolved contradictions may change requirement wording"
      pattern: "requirement.*update\\|update.*requirement"
---

<objective>
Add spec-vs-research validation step that surfaces contradictions between spec assumptions and research findings.

Purpose: After research completes (Step 10) and before requirements generation (Step 11), the workflow should compare spec content against research findings, classify contradictions as major or minor, pause for user input on major ones, and flow resolutions through to downstream artifacts. This covers PIPE-07.

Output: Updated `new-project-from-spec.md` workflow with a new Step 10.5 for contradiction detection and resolution.
</objective>

<execution_context>
@/home/andreas/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/andreas/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-brownfield-validation/03-CONTEXT.md
@.planning/phases/03-brownfield-validation/03-01-SUMMARY.md

Key references to read before implementing:
- get-shit-done/workflows/new-project-from-spec.md (the file being modified — read the FULL file, including Plan 01 changes)
- The CONTEXT.md decisions section on contradiction surfacing, validation strictness, and resolution options
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Step 10.5 — Spec-vs-Research Validation</name>
  <files>get-shit-done/workflows/new-project-from-spec.md</files>
  <action>
  Insert a new step between Step 10e (research completion display) and Step 11 (Generate Requirements). This is the entirely new contradiction surfacing feature (PIPE-07). There is NO existing pattern to mirror — this is new logic.

  **Insert as "## 10.5 Validate Spec Assumptions Against Research" (or renumber to fit cleanly — e.g., make it Step 10f and 10g within the existing Step 10 section).**

  Content of the new step:

  ### 10f. Compare Spec Assumptions Against Research Findings

  Read and cross-reference:
  - `.planning/PROJECT.md` (spec-derived assumptions — tech choices, architecture, constraints, key decisions)
  - `.planning/research/SUMMARY.md` (synthesized research findings)
  - `.planning/research/STACK.md` (tech stack recommendations)
  - `.planning/research/ARCHITECTURE.md` (architecture patterns)
  - `.planning/research/PITFALLS.md` (common mistakes)

  Identify contradictions across four categories (from CONTEXT.md locked decisions):
  1. **Tech choice conflicts** — spec specifies a technology, research recommends something different
  2. **Deprecated/risky dependencies** — spec assumes a library or tool that research flags as deprecated, unmaintained, or risky
  3. **Architectural mismatches** — spec describes an architecture, research shows a different pattern is standard/better for the domain
  4. **Feasibility concerns** — spec assumes something is straightforward, research indicates significant complexity or impossibility

  For each contradiction found:
  - Extract the **spec quote** (specific text from PROJECT.md with `<!-- from: file.md -->` attribution)
  - Extract the **research finding** (specific text from research files)
  - Classify as **major** or **minor** using both factors (CONTEXT.md locked decision):
    - **Major:** High impact (architecture/core tech) AND high research confidence (strong contradiction, not just a defensible alternative) → blocks pipeline
    - **Minor:** Low impact OR low confidence (research suggests alternative but spec choice is defensible) → noted but doesn't block

  ### 10g. Surface Contradictions

  **If no contradictions found:** Continue silently to Step 11.

  **If only minor contradictions found:**
  - Display: `Research found {N} minor note(s) about your spec assumptions (non-blocking).`
  - Add each minor contradiction as a flag in PROJECT.md Key Decisions table:
    - Decision: the spec assumption
    - Rationale: `Research disagrees: {research finding}` with `<!-- from: research/STACK.md -->` attribution
    - Outcome: `⚠️ Revisit`
  - Continue to Step 11 without blocking.

  **If major contradictions found:**
  - Display grouped summary (CONTEXT.md locked decision: "grouped summary"):

  ```
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   GSD ► VALIDATING SPECS
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Research found {N} contradiction(s) with your specs:
  ```

  - For each major contradiction, present with AskUserQuestion:
    - header: "Spec Contradiction {N}/{total}"
    - question: Show spec quote + research finding side by side:
      ```
      **Your spec says:** "{spec quote}" (from {spec-file.md})
      **Research found:** "{research finding}" (from research/{file}.md)
      ```
    - options:
      - "Keep spec" — Use the spec assumption as-is
      - "Accept research" — Use the research recommendation instead
      - "Custom" — Provide your own resolution

  - **If "Custom":** Use AskUserQuestion with freeform text input to get the user's custom resolution.

  ### 10h. Apply Resolutions

  For each resolved major contradiction:

  **"Keep spec":**
  - Add to PROJECT.md Key Decisions table:
    - Decision: the spec assumption
    - Rationale: `Kept despite research disagreement: {research finding}`
    - Outcome: `— Confirmed`

  **"Accept research":**
  - Update PROJECT.md to replace the spec assumption with the research recommendation
  - Update the Key Decisions table:
    - Decision: the new research-based choice
    - Rationale: `Changed from spec ({original}) based on research: {finding}`
    - Outcome: `— Updated`
  - If this changes a requirement in the Active list, update the requirement text to reflect the new choice

  **"Custom":**
  - Update PROJECT.md with the user's custom answer replacing the spec assumption (CONTEXT.md locked decision: "custom answer replaces the original spec assumption")
  - Update the Key Decisions table:
    - Decision: the custom choice
    - Rationale: `Custom resolution (spec: {original}, research: {finding})`
    - Outcome: `— Custom`
  - If this changes a requirement, update the requirement text

  After all resolutions applied:
  ```
  {N} contradiction(s) resolved. Continuing to requirements...
  ```

  **IMPORTANT:** The existing decision from Phase 2 still applies: "Research findings inform only — requirements come from specs. Research shapes HOW requirements are written, not WHAT they are." Contradiction surfacing EXTENDS this: research can now CHALLENGE spec assumptions, but only through explicit user resolution (not silently).

  Also add minor contradictions as flags (same as the minor-only path above).

  Continue to Step 11.
  </action>
  <verify>
  Read the new steps (10f, 10g, 10h). Verify:
  1. Four contradiction categories defined (tech conflicts, deprecated deps, arch mismatches, feasibility)
  2. Each contradiction has spec quote + research finding with source attribution
  3. Major/minor classification uses both impact AND confidence
  4. Minor contradictions added as Key Decisions flags without blocking
  5. Major contradictions presented with AskUserQuestion
  6. Three resolution options: Keep spec / Accept research / Custom
  7. Each resolution path updates PROJECT.md appropriately
  8. Custom resolutions flow through to requirements
  9. Step is positioned between 10e and 11
  10. Existing "research informs only" principle respected — contradictions are the exception path
  </verify>
  <done>New validation step detects contradictions between specs and research, classifies major/minor, prompts user for major resolutions, and flows all changes into downstream artifacts</done>
</task>

<task type="auto">
  <name>Task 2: Update workflow metadata and success criteria</name>
  <files>get-shit-done/workflows/new-project-from-spec.md</files>
  <action>
  Update the workflow's bookkeeping sections to reflect the new brownfield + validation capabilities:

  **A. Update the `<purpose>` section** (lines 1-16):
  Add mentions of brownfield handling and spec-vs-research validation to the purpose description and numbered list. Insert between items 5 (config extraction) and 6 (research):
  - After item 4, add: "4b. For brownfield projects, extracts existing capabilities as Validated requirements"
  - After item 6 (research), add: "6b. Validates spec assumptions against research, surfacing contradictions"
  
  Or renumber the full list cleanly to include these as first-class steps.

  **B. Update the `<output>` section** (around line 1057):
  No new files are created — the existing output list covers PROJECT.md, REQUIREMENTS.md, etc. But add a note that for brownfield projects, PROJECT.md will contain Validated requirements and an "Existing Capabilities" section.

  **C. Update the `<success_criteria>` section** (around line 1074):
  Add these criteria:
  - `- [ ] Brownfield detection offers mapping options when codebase map exists`
  - `- [ ] Existing capabilities extracted and merged as Validated in PROJECT.md (brownfield)`
  - `- [ ] Codebase context fed to research agents (brownfield)`
  - `- [ ] Spec assumptions validated against research findings`
  - `- [ ] Major contradictions surfaced to user with resolution options`
  - `- [ ] Minor contradictions flagged in Key Decisions (non-blocking)`
  - `- [ ] Resolved contradictions reflected in PROJECT.md and REQUIREMENTS.md`

  **D. Update Step 13b (completion summary):**
  Add brownfield info to the completion display when applicable:
  - Add row: `| Validated capabilities | {N} from codebase |` (only when brownfield)
  - Add row: `| Contradictions resolved | {N} major, {M} minor |` (only when contradictions found)
  </action>
  <verify>
  Read the purpose, output, success_criteria, and Step 13b sections. Verify:
  1. Purpose mentions brownfield handling and spec validation
  2. Output section notes brownfield-specific PROJECT.md content
  3. Success criteria include brownfield and validation checks
  4. Completion summary shows validated capabilities count (brownfield)
  5. Completion summary shows contradiction counts (when applicable)
  </verify>
  <done>Workflow metadata accurately reflects the new brownfield and validation capabilities added in Plan 01 and Task 1 of this plan</done>
</task>

</tasks>

<verification>
After both tasks, read the full workflow file and verify:
1. New Step 10f/10g/10h exists between research completion and requirements generation
2. Four contradiction categories covered
3. Major contradictions block with AskUserQuestion (3 resolution options)
4. Minor contradictions noted without blocking
5. Resolutions update PROJECT.md Key Decisions table
6. Custom resolutions flow through to requirements
7. Purpose, output, and success_criteria sections updated
8. Completion summary includes brownfield and validation stats
9. The overall flow is coherent: Step 3 → 3b → ... → 7 → ... → 10 → 10f → 10g → 10h → 11 → ... → 13
</verification>

<success_criteria>
- Spec-vs-research validation step exists and is properly positioned in the workflow
- Contradictions classified as major (blocking) or minor (flagged)
- Major contradictions presented with spec quote + research finding + source attribution
- Three resolution options work correctly (Keep / Accept / Custom)
- All resolutions reflected in PROJECT.md Key Decisions table
- Custom and Accept resolutions update requirement text when applicable
- Workflow metadata (purpose, output, success_criteria) reflects new capabilities
- Completion summary shows validation results
</success_criteria>

<output>
After completion, create `.planning/phases/03-brownfield-validation/03-02-SUMMARY.md`
</output>
