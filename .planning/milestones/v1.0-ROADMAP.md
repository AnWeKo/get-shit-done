# Milestone v1.0: GSD Spec-Based Project Initialization

**Status:** SHIPPED 2026-02-22
**Phases:** 1-4
**Total Plans:** 9

## Overview

This project adds a `/gsd-new-project-from-spec` command that reads a folder of markdown spec files and produces the same planning artifacts as the interactive `/gsd-new-project` flow — fully automated, no approval gates. The work progresses from getting the command to read and synthesize specs (Phase 1), through wiring the full artifact pipeline (Phase 2), adding brownfield and research validation support (Phase 3), to multi-runtime verification and polish (Phase 4).

## Phases

### Phase 1: Spec Reading & Synthesis

**Goal**: User can run the command and get a valid PROJECT.md synthesized from their spec files
**Depends on**: Nothing (first phase)
**Requirements**: CMD-01, CMD-02, CMD-04, SPEC-01, SPEC-02, SPEC-03, SPEC-04, SPEC-05, PIPE-01
**Success Criteria**:
  1. User can run `/gsd-new-project-from-spec` (or `/gsd-new-project-from-spec ./my-specs/`) and the command starts the workflow
  2. Workflow reads all `.md` files from the spec folder, or shows a clear error if the folder is missing/empty
  3. Workflow produces a PROJECT.md that contains synthesized project context, requirements, constraints, and decisions extracted from the spec files — in the same format as the interactive flow
  4. When spec files contain contradictions or critical gaps, the workflow asks the user targeted questions instead of silently guessing
  5. Each spec file's role (PRD, tech spec, user stories, etc.) is identified and weighted during synthesis
**Plans:** 2 plans

Plans:
- [x] 01-01-PLAN.md — Command entry point & spec reading engine (validation, file reading, classification)
- [x] 01-02-PLAN.md — Spec synthesis & PROJECT.md generation (conflict detection, gap handling, template-compliant output)

**Details:**
- Created `/gsd-new-project-from-spec` slash command following existing command patterns
- Workflow with Steps 1-7: initialization, `.planning/` detection, brownfield offer, spec folder validation, semantic file classification, synthesis with conflict detection, PROJECT.md generation
- 9 classification roles for spec file types (PRD, Technical Spec, User Stories, Architecture, Constraints, API, Data Model, UI/UX, Other)
- Major/minor contradiction classification — major asks user, minor auto-resolves
- Batched user questions with file+quote citations
- INFERABLE assumptions documented in Key Decisions with Revisit flag
- Supplementary content preserved in .planning/spec-references/

### Phase 2: Pipeline Automation

**Goal**: One command produces all planning artifacts (PROJECT.md → config.json → research → REQUIREMENTS.md → ROADMAP.md → STATE.md) and commits everything automatically
**Depends on**: Phase 1
**Requirements**: PIPE-02, PIPE-03, PIPE-04, PIPE-05, PIPE-06, PIPE-08, PIPE-09
**Success Criteria**:
  1. Running the command on a spec folder produces all 6 planning artifacts without user intervention
  2. Config preferences are extracted from spec prose (e.g., "move fast" → quick depth, "mission-critical" → comprehensive depth)
  3. Research phase runs automatically (4 parallel researchers + synthesizer) and findings inform generated requirements
  4. Generated REQUIREMENTS.md has domain-specific categories, proper REQ-IDs, and full traceability to roadmap phases
  5. All generated artifacts are committed to git automatically with no approval gates
**Plans:** 3 plans

Plans:
- [x] 02-01-PLAN.md — Config extraction from spec prose (inference, citations, per-value choices, config.json generation)
- [x] 02-02-PLAN.md — Research execution & requirements generation (4 parallel researchers, REQUIREMENTS.md with REQ-IDs)
- [x] 02-03-PLAN.md — Roadmap generation, atomic commit & completion (roadmapper, auto-commit all artifacts, summary)

**Details:**
- Config extraction with conservative inference — ambiguous signals default to asking user
- Signal-to-config mapping table for natural language to config value translation
- Research topics derived from spec content, not generic questions
- Requirements come from specs only; research informs wording precision
- REQ-IDs use [CATEGORY]-[NUMBER] format with domain-specific categories
- No approval gate after roadmap — fully automated (PIPE-08)
- Atomic commit includes all 6 artifacts plus spec-references if present

### Phase 3: Brownfield & Validation

**Goal**: The command correctly handles existing codebases and validates spec assumptions against research findings
**Depends on**: Phase 2
**Requirements**: BRWN-01, BRWN-02, PIPE-07
**Success Criteria**:
  1. When run in a directory with existing code, the workflow detects the codebase and offers mapping before initialization
  2. Existing codebase capabilities appear as Validated requirements in the generated PROJECT.md
  3. Research findings that contradict spec assumptions are surfaced to the user with specific references to the conflicting spec content
**Plans:** 2 plans

Plans:
- [x] 03-01-PLAN.md — Brownfield codebase detection & capability merging (BRWN-01, BRWN-02)
- [x] 03-02-PLAN.md — Spec-vs-research validation & contradiction surfacing (PIPE-07)

**Details:**
- Three-branch brownfield detection: existing map (use/refresh), no map but existing code (map/skip), greenfield (silent continue)
- Validated capabilities auto-marked without user confirmation
- Partial overlaps split into Validated + Active line items
- All 4 researchers receive codebase context conditionally
- Four contradiction categories: tech conflicts, deprecated deps, arch mismatches, feasibility
- Major = high impact AND high confidence; everything else = minor
- Three resolution options: Keep spec / Accept research / Custom
- Resolution ripple: updates flow through PROJECT.md and REQUIREMENTS.md

### Phase 4: Multi-Runtime & Polish

**Goal**: The command works reliably across all supported runtimes and handles edge cases gracefully
**Depends on**: Phase 3
**Requirements**: CMD-03
**Success Criteria**:
  1. `/gsd-new-project-from-spec` works in Claude Code, OpenCode, and Gemini CLI
  2. Large spec folders (>100KB total) are handled without token limit failures
  3. End-to-end test: realistic spec folder produces artifacts compatible with downstream GSD commands
**Plans:** 2 plans

Plans:
- [x] 04-01-PLAN.md — Large spec folder resilience & edge case handling (content budgeting, priority tiers, empty/encoding edge cases)
- [x] 04-02-PLAN.md — Cross-runtime verification & E2E test fixture (installer output checks, test fixture, automated tests)

**Details:**
- 100KB threshold triggers content budget; 3 priority tiers across 9 classification roles
- Tier 1 (PRD, tech spec, constraints) always read in full
- Summarized file notices only shown during contradiction resolution
- Edge cases: empty .md files skipped, encoding errors handled gracefully
- Realistic 3-file test fixture with intentional contradiction
- 8 new tests (cross-runtime source validation + artifact parseability)
- Test suite: 89 passing, 0 failing

## Progress

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 1. Spec Reading & Synthesis | 2/2 | Complete | 2026-02-21 |
| 2. Pipeline Automation | 3/3 | Complete | 2026-02-22 |
| 3. Brownfield & Validation | 2/2 | Complete | 2026-02-22 |
| 4. Multi-Runtime & Polish | 2/2 | Complete | 2026-02-22 |

---

## Milestone Summary

**Decimal Phases:**

None — all phases were integer phases as originally planned.

**Key Decisions:**

- Reuse existing `init new-project` CLI command rather than creating a new one
- Top-level only file reading (not recursive) for spec folder
- 9 classification roles covering common spec document types
- Major contradictions require user input; minor differences auto-resolve
- All conflict/gap questions batched with file+quote citations
- INFERABLE assumptions documented in Key Decisions with Revisit flag
- workflow.research always true for spec-from-file flow
- Conservative inference: ambiguous signals default to asking user
- Requirements come from specs only; research informs wording precision
- REQ-IDs use [CATEGORY]-[NUMBER] format with domain-specific categories
- No approval gate after roadmap — fully automated (PIPE-08)
- Validated capabilities auto-marked without user confirmation
- 100KB threshold triggers content budget; 3 priority tiers
- Source file validation verifies installer-convertible structure without calling installer

**Issues Resolved:**

- Phase 2 VERIFICATION flagged stale command metadata — fixed in Phase 4 (command now accurately describes full pipeline)

**Issues Deferred:**

- Spec file caching for incremental re-reads (CACHE-01) — deferred to v2
- Informational spec quality scoring (SCORE-01) — deferred to v2

**Technical Debt Incurred:**

None — no critical tech debt. All verification items passed.

---

_Archived: 2026-02-22 as part of v1.0 milestone completion_
