---
phase: 04-multi-runtime-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/spec-e2e/prd.md
  - tests/fixtures/spec-e2e/tech-spec.md
  - tests/fixtures/spec-e2e/user-stories.md
  - tests/e2e-spec-from-file.test.cjs
autonomous: true

must_haves:
  truths:
    - "The new-project-from-spec command and workflow source files are correctly structured for all 3 runtimes"
    - "A realistic test fixture exists that can exercise the full spec-from-file pipeline"
    - "An automated test verifies source file structure is compatible with the installer's conversion functions for Claude Code, OpenCode, and Gemini"
    - "An automated test validates that mock pipeline output artifacts can be parsed by gsd-tools.cjs"
  artifacts:
    - path: "tests/fixtures/spec-e2e/prd.md"
      provides: "Minimal PRD spec for E2E testing"
    - path: "tests/fixtures/spec-e2e/tech-spec.md"
      provides: "Minimal tech spec for E2E testing"
    - path: "tests/fixtures/spec-e2e/user-stories.md"
      provides: "Minimal user stories for E2E testing"
    - path: "tests/e2e-spec-from-file.test.cjs"
      provides: "Automated cross-runtime and artifact structure verification"
  key_links:
    - from: "tests/e2e-spec-from-file.test.cjs"
      to: "commands/gsd/new-project-from-spec.md"
      via: "Tests verify source file structure is conversion-safe"
      pattern: "frontmatter|allowed-tools|name:"
    - from: "tests/e2e-spec-from-file.test.cjs"
      to: "get-shit-done/bin/gsd-tools.cjs"
      via: "Tests use gsd-tools to validate artifact parseability"
      pattern: "gsd-tools|frontmatter|roadmap"
---

<objective>
Verify cross-runtime compatibility and create an E2E test fixture with artifact validation.

Purpose: CMD-03 requires the command works in all 3 runtimes. The installer already does generic conversion — we verify the new-project-from-spec source files are conversion-safe. For SC3 (E2E downstream compatibility), the full pipeline requires an LLM and can't be automated in a test suite, so we create a test fixture AND validate that correctly-structured artifacts are parseable by downstream GSD tooling.

Output: Test fixture in tests/fixtures/spec-e2e/, automated test covering cross-runtime source validation and artifact parseability.
</objective>

<execution_context>
@/home/andreas/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/andreas/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-runtime-polish/04-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test fixture spec files</name>
  <files>
    tests/fixtures/spec-e2e/prd.md
    tests/fixtures/spec-e2e/tech-spec.md
    tests/fixtures/spec-e2e/user-stories.md
  </files>
  <action>
  Create a minimal but realistic spec folder in `tests/fixtures/spec-e2e/` that can exercise the full spec-from-file pipeline. Per CONTEXT.md: "Minimal — 2-3 files (PRD and tech spec at minimum), just enough to prove the pipeline produces all 6 artifacts."

  Create 3 files for a simple fictional project (e.g., a task management CLI):

  **prd.md** (~1-2KB):
  - Project name, vision, target users
  - 3-4 core features (enough to generate multiple requirements/phases)
  - Success metrics
  - Include one slightly ambiguous requirement that could trigger the gap detection system

  **tech-spec.md** (~1-2KB):
  - Technology choices (e.g., Node.js, SQLite)
  - Architecture overview (CLI with local storage)
  - Key constraints (e.g., offline-first, no cloud dependencies)
  - Include one mild contradiction with prd.md (e.g., PRD mentions "real-time sync" but tech spec says "offline-first") to exercise contradiction detection

  **user-stories.md** (~0.5-1KB):
  - 3-4 user stories in "As a user, I want..." format
  - Each with acceptance criteria
  - Straightforward content, no contradictions

  The fixture should be:
  - Small enough to run quickly in tests (~3-4KB total)
  - Realistic enough to exercise synthesis, classification, contradiction detection, and gap handling
  - Self-contained (no external references)
  </action>
  <verify>
  ```bash
  ls -la tests/fixtures/spec-e2e/
  wc -c tests/fixtures/spec-e2e/*.md
  ```
  Verify: 3 .md files exist, total size ~3-5KB. Each file has meaningful content (not stubs).
  </verify>
  <done>
  Three spec fixture files exist in tests/fixtures/spec-e2e/ with realistic content including at least one contradiction between files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create automated cross-runtime and artifact validation tests</name>
  <files>tests/e2e-spec-from-file.test.cjs</files>
  <action>
  Create an automated test file using Node.js built-in test runner (`node:test` + `node:assert`), following the same patterns as existing tests in `tests/*.test.cjs`. Import helpers from `tests/helpers.cjs` for `runGsdTools`.

  **The test covers two concerns:**

  ### Part A: Cross-Runtime Source Validation (CMD-03)

  The installer's conversion functions are NOT exported, so we can't call them directly. Instead, verify the **source files** are well-formed and conversion-safe. The installer converts ALL commands/workflows/agents generically — if the source files follow established patterns, they convert correctly (proven by all other commands working). Test:

  1. **"command source file has valid frontmatter"** — Read `commands/gsd/new-project-from-spec.md`, verify:
     - Has YAML frontmatter block (starts with `---`, ends with `---`)
     - Contains `name:` field
     - Contains `allowed-tools:` field (Claude Code format, converted to `tools:` for OpenCode)
     - Contains `description:` field
  
  2. **"workflow source file exists and is substantial"** — Read `get-shit-done/workflows/new-project-from-spec.md`, verify:
     - File exists and is >500 lines
     - Contains `<purpose>` tag (required for all workflows)
     - Contains `<process>` tag

  3. **"command has no raw template variables outside bash blocks"** — Read the command file and verify no `${VAR}` patterns outside of bash code blocks (these would break Gemini conversion). Regex check: any `${...}` not inside triple-backtick blocks should flag. Skip `$ARGUMENTS` which is intentional.

  4. **"workflow path references use canonical form"** — Read the workflow file and verify all path references use `~/.claude/` (the canonical form that the installer converts per-runtime). No hardcoded `~/.opencode/` or `~/.gemini/` references.

  ### Part B: Artifact Structure Validation (SC3 — E2E downstream compatibility)

  The full pipeline requires an LLM, so it can't run in a test suite. Instead, create **mock artifacts** that match the expected output structure, then verify GSD tooling can parse them. This proves that correctly-structured pipeline output is compatible with downstream commands.

  5. **"gsd-tools can parse valid ROADMAP.md structure"** — Create a temp project directory with a minimal ROADMAP.md (following the template format from `get-shit-done/templates/roadmap.md`) and verify `node gsd-tools.cjs roadmap phases` returns valid JSON with phase data.

  6. **"gsd-tools can parse valid STATE.md structure"** — Create a temp project with a minimal STATE.md and verify `node gsd-tools.cjs state snapshot` returns valid JSON.

  7. **"gsd-tools can validate frontmatter in PLAN.md"** — Create a temp project with a minimal PLAN.md and verify `node gsd-tools.cjs frontmatter get plan --file <path>` returns a valid value.

  8. **"test fixture spec files are valid"** — Verify the 3 fixture files from Task 1 exist, are non-empty, and contain markdown content (have headings).

  **Implementation pattern for Part B tests:**
  ```javascript
  const tmpDir = createTempProject();  // from helpers.cjs
  // Write minimal artifact file to tmpDir/.planning/
  fs.writeFileSync(path.join(tmpDir, '.planning', 'ROADMAP.md'), minimalRoadmap);
  // Use runGsdTools to parse it
  const result = runGsdTools('roadmap phases', tmpDir);
  assert.ok(result.success);
  const parsed = JSON.parse(result.output);
  assert.ok(parsed.phases.length > 0);
  cleanup(tmpDir);
  ```

  Use `beforeEach`/`afterEach` for temp directory setup/cleanup. Group tests into two `describe` blocks: "cross-runtime source validation" and "artifact structure validation".
  </action>
  <verify>
  ```bash
  node --test tests/e2e-spec-from-file.test.cjs
  ```
  All tests pass. Also run the full test suite to verify no regressions:
  ```bash
  npm test
  ```
  All existing tests still pass (81+ baseline).
  </verify>
  <done>
  Test file has two sections: cross-runtime source validation (4 tests verifying command/workflow files are conversion-safe) and artifact structure validation (4 tests verifying GSD tooling can parse correctly-structured artifacts). All tests pass. Full test suite still passes.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/e2e-spec-from-file.test.cjs` passes
- `npm test` still passes (no regressions)
- Test fixture files are realistic and exercise key pipeline features
- Cross-runtime tests verify source files follow installer conventions
- Artifact tests verify downstream GSD tooling can parse pipeline output
</verification>

<success_criteria>
- Test fixture exists at tests/fixtures/spec-e2e/ with 3 realistic spec files
- Cross-runtime source validation tests pass (command frontmatter, workflow structure, no template variable leaks, canonical paths)
- Artifact structure validation tests pass (ROADMAP parsed, STATE parsed, PLAN frontmatter parsed)
- All new tests pass
- All existing tests still pass (81+ passing baseline)
- CMD-03 addressed through source validation
- SC3 addressed through artifact parseability verification
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-runtime-polish/04-02-SUMMARY.md`
</output>
