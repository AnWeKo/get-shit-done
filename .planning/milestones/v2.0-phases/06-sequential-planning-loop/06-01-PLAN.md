---
phase: 06-sequential-planning-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/plan-all.md
  - commands/gsd/plan-all.md
autonomous: true
requirements: [ORCH-02, ORCH-03, ORCH-04, ORCH-05, STAT-01]

must_haves:
  truths:
    - "Running /gsd-plan-all on a project with unplanned phases plans each one sequentially"
    - "Phases are planned in ascending phase-number order"
    - "Each completed phase plan is committed before the next phase begins"
    - "User sees progress updates as each phase completes (Phase N of M)"
    - "STATE.md is updated with current phase, status, and last activity during batch planning"
  artifacts:
    - path: "get-shit-done/workflows/plan-all.md"
      provides: "Sequential planning loop replacing Step 5 placeholder"
      contains: "## 5. Sequential Planning Loop"
    - path: "commands/gsd/plan-all.md"
      provides: "Updated command description reflecting implemented loop"
  key_links:
    - from: "get-shit-done/workflows/plan-all.md"
      to: "get-shit-done/workflows/plan-phase.md"
      via: "Task subagent spawn per phase"
      pattern: "plan-phase\\.md"
    - from: "get-shit-done/workflows/plan-all.md"
      to: "gsd-tools.cjs state patch"
      via: "CLI call to update STATE.md"
      pattern: "state patch"
    - from: "get-shit-done/workflows/plan-all.md"
      to: "gsd-tools.cjs commit-docs"
      via: "CLI call to commit after each phase plan"
      pattern: "commit-docs"
---

<objective>
Replace the Step 5 placeholder in plan-all.md with a working sequential planning loop that iterates unplanned phases, spawns plan-phase for each, commits results, updates STATE.md, and shows progress.

Purpose: This is the core orchestration for `/gsd-plan-all` — the feature that lets one command batch-plan all roadmap phases with zero interaction.
Output: Updated `plan-all.md` workflow with complete sequential loop, updated command file, updated offer_next and success_criteria.
</objective>

<execution_context>
@/home/andreas/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/andreas/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-command-phase-discovery/05-01-SUMMARY.md
@.planning/phases/05-command-phase-discovery/05-02-SUMMARY.md
@get-shit-done/workflows/plan-all.md
@get-shit-done/workflows/plan-phase.md
@get-shit-done/bin/lib/state.cjs
@get-shit-done/bin/lib/init.cjs
@commands/gsd/plan-all.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Step 5 placeholder with sequential planning loop</name>
  <files>get-shit-done/workflows/plan-all.md</files>
  <action>
Replace the entire Step 5 section (from `<!-- Phase 6 replaces this section -->` through the placeholder text) with a new `## 5. Sequential Planning Loop` section. The replacement must:

**Loop structure:**
```
For each phase in `unplanned_phases` (already sorted by phase number from init):
  1. Display phase banner: "GSD > PLANNING PHASE {N} ({current}/{total})"
  2. Update STATE.md via: `node gsd-tools.cjs state patch --"Current focus" "Phase {N} — {Name}" --"Status" "Batch planning" --"Last activity" "{date} — Planning phase {N}"`
  3. Spawn plan-phase via Task subagent:
     - Use subagent_type="general"
     - Prompt must reference plan-phase.md workflow and pass the phase number
     - Include flags: --skip-research (if `research_enabled` is false from init), --skip-verify (if `plan_checker_enabled` is false from init)
     - The Task prompt must instruct the agent to read plan-phase.md as its workflow and execute it end-to-end
     - Do NOT use the Skill tool — use direct @file reference to plan-phase.md
  4. Handle plan-phase return:
     - "PLANNING COMPLETE": Increment success counter, display "Phase {N} planned" with checkmark
     - "CHECKPOINT REACHED": Display warning that batch mode skips checkpoints, count as planned
     - "PLANNING INCONCLUSIVE" or error: Increment failure counter, display warning, continue to next phase
  5. Commit planning docs: `node gsd-tools.cjs commit-docs "docs(phase-{N}): batch plan phase {N}"`
  6. Display progress: "Progress: {completed}/{total} phases planned"
```

**After loop completes:**
- Display completion banner: "GSD > BATCH PLANNING COMPLETE"
- Show summary table: | Phase | Name | Result | (checkmark/X for each)
- Update STATE.md with final state: `state patch --"Status" "Batch planning complete" --"Last activity" "{date} — Batch planned {N} phases"`
- If any failures, display: "{N} phase(s) had issues — plan manually: /gsd-plan-phase {phase_number}"

**Important implementation details:**
- The workflow is a markdown prompt, NOT executable code. Write it as instructions for an AI orchestrator to follow step by step.
- Use the same `node ~/.claude/get-shit-done/bin/gsd-tools.cjs` path pattern as the rest of the workflow (Steps 1-4 already use this).
- The Task spawn for plan-phase must match the pattern used in plan-phase.md's own auto-advance (Step 14) — reference the workflow file directly.
- Each phase planning happens in a fresh Task (subagent), which means each gets a fresh context window. This is intentional.
- The `unplanned_phases` array from init JSON contains objects with: `number`, `name`, `goal`, `disk_status`, `has_context`, `has_research`. Use these fields in the loop.

Also update the `<offer_next>` block at the bottom of the file:
- Replace the current "DISCOVERY COMPLETE" banner with "BATCH PLANNING COMPLETE"
- Show the summary table with phase results
- Next up should suggest `/gsd-execute-phase {first_phase}` if all phases planned successfully
- Include alternative: `/gsd-plan-phase {N}` for any failed phases

Also update the `<success_criteria>` block:
- Add criteria for: sequential loop executed, each phase planned, STATE.md updated, commits made, progress displayed
- Keep the existing discovery criteria (Steps 1-4 unchanged)
  </action>
  <verify>
    <automated>grep -c "Sequential Planning Loop" get-shit-done/workflows/plan-all.md | grep -q "1" && grep -c "Phase 6 replaces this section" get-shit-done/workflows/plan-all.md | grep -q "0" && grep -c "state patch" get-shit-done/workflows/plan-all.md | grep -qv "0" && grep -c "commit-docs" get-shit-done/workflows/plan-all.md | grep -qv "0" && echo "PASS" || echo "FAIL"</automated>
    <manual>Read the updated plan-all.md and verify: Step 5 has loop logic, references plan-phase.md, calls state patch, calls commit-docs, shows progress, has completion summary</manual>
  </verify>
  <done>
Step 5 placeholder fully replaced with sequential planning loop. The loop iterates unplanned phases in order, spawns plan-phase per phase, commits after each, updates STATE.md, and shows progress. offer_next and success_criteria updated for batch completion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update command file and validate workflow consistency</name>
  <files>commands/gsd/plan-all.md</files>
  <action>
Update `commands/gsd/plan-all.md`:

1. Update the `<objective>` section to remove "(Phase 6 adds the loop)" parenthetical — the loop is now implemented. Replace with a clear description: "Discovers which phases need planning, then plans each one sequentially using the existing plan-phase pipeline."

2. Update the `<process>` section description if it references Phase 6 as future work.

3. Leave frontmatter (name, description, argument-hint, agent, allowed-tools) unchanged — they are already correct.

Then validate the full workflow is internally consistent:
- Verify Step 5 references variables set in Steps 1-3 (`unplanned_phases`, `unplanned_count`, `research_enabled`, `plan_checker_enabled`, `commit_docs`)
- Verify no dangling references to the old placeholder
- Verify the loop uses the same `~/.claude/get-shit-done/` path prefix as Steps 1-4
  </action>
  <verify>
    <automated>grep -c "Phase 6 adds" commands/gsd/plan-all.md | grep -q "0" && grep -c "plan-phase pipeline" commands/gsd/plan-all.md | grep -qv "0" && echo "PASS" || echo "FAIL"</automated>
    <manual>Read command file, confirm no references to Phase 6 as future work, description matches implemented functionality</manual>
  </verify>
  <done>
Command file description matches implemented functionality. No dangling references to Phase 6 placeholder. Workflow is internally consistent end-to-end.
  </done>
</task>

</tasks>

<verification>
1. `grep "Sequential Planning Loop" get-shit-done/workflows/plan-all.md` — Step 5 heading exists
2. `grep -v "Phase 6 replaces" get-shit-done/workflows/plan-all.md | wc -l` — placeholder comment removed (line count should match total)
3. `grep "plan-phase.md" get-shit-done/workflows/plan-all.md` — references plan-phase workflow
4. `grep "state patch" get-shit-done/workflows/plan-all.md` — STATE.md update calls present
5. `grep "commit-docs" get-shit-done/workflows/plan-all.md` — git commit calls present
6. `grep "BATCH PLANNING COMPLETE" get-shit-done/workflows/plan-all.md` — completion banner present
7. No placeholder text remains: `grep "placeholder\|not yet implemented" get-shit-done/workflows/plan-all.md` should return nothing
</verification>

<success_criteria>
- plan-all.md Step 5 contains a sequential loop that iterates unplanned phases
- Each phase spawns plan-phase as a Task subagent with correct flags
- STATE.md is updated via `state patch` before each phase and after completion
- `commit-docs` is called after each successful phase plan
- Progress display shows "Phase N of M" as each completes
- Completion summary shows table of results per phase
- Failed phases don't abort the loop — they are reported at the end
- offer_next reflects batch completion state
- Command file description matches implemented functionality
- No placeholder or "Phase 6" future-work references remain
</success_criteria>

<output>
After completion, create `.planning/phases/06-sequential-planning-loop/06-01-SUMMARY.md`
</output>
