---
phase: 04-multi-runtime-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/new-project-from-spec.md
autonomous: true

must_haves:
  truths:
    - "Large spec folders (>100KB total) are handled without token limit failures"
    - "High-priority spec files (PRD, tech spec, constraints) are always read in full regardless of total size"
    - "Lower-priority spec files are summarized when the total content exceeds 100KB"
    - "User is only notified about summarized files when those files had conflicts or ambiguities"
    - "Non-markdown files in spec folder are noted and skipped with a clear message"
    - "Empty .md files (0 bytes or whitespace only) are skipped silently"
  artifacts:
    - path: "get-shit-done/workflows/new-project-from-spec.md"
      provides: "Large spec folder resilience and edge case handling in Steps 4-6"
      contains: "100KB"
  key_links:
    - from: "Step 4 (file reading)"
      to: "Step 5 (classification) then budget trimming"
      via: "Content budget gate applied after classification uses roles to determine priorities"
      pattern: "100.*KB|priority.*order|summariz"
---

<objective>
Add large spec folder resilience and edge case handling to the new-project-from-spec workflow.

Purpose: Spec folders over 100KB would overwhelm the context window during synthesis. This plan adds a priority-based content budgeting system that reads high-priority files (PRD, tech spec, constraints) in full and summarizes lower-priority ones when over budget. It also hardens edge case handling (non-markdown files, empty files, encoding issues).

Output: Updated workflow with content budgeting across Steps 4-6, plus edge case handling in Step 4.
</objective>

<execution_context>
@/home/andreas/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/andreas/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-runtime-polish/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add content budget system and edge case handling to workflow Steps 4-6</name>
  <files>get-shit-done/workflows/new-project-from-spec.md</files>
  <action>
  This is a single cohesive change to the workflow across Steps 4, 5, and 6. All changes are to the same file and are tightly coupled — the budget gate in Step 4 depends on classification in Step 5, and the notification rule applies in Step 6.

  **Step 4 changes ("Spec Folder Validation and File Reading"):**

  1. **Edge case handling** — add these three instructions to the file reading sub-step (after sub-step 3 "Read each file's full content"):
     - "Skip .md files with no content (0 bytes or whitespace only) — do not count, list, or classify them."
     - "If a file cannot be read cleanly (encoding errors, binary content), read what's readable and continue. Do not abort the workflow for a single unreadable file."
     - Update the non-markdown file warning wording to: `⚠ Skipping {N} non-markdown files ({comma-separated list up to 5 names}...)`

  2. **Content budget gate** — add a new sub-step after reading all files and before the file list display table:

     **"Content Budget Check":**
     - Calculate total content size across all read .md files (sum of character counts).
     - If total <= 100KB: proceed normally — all files used in full. No budget intervention.
     - If total > 100KB: display `⚠ Large spec folder ({X} KB total) — applying content budget`. Set a flag `content_budget_active = true` for use after Step 5 classification.
     - Note: "The actual content trimming happens after Step 5 classification, because classification roles determine which files are high-priority. See Step 5b."

  **Step 5 changes ("Spec File Classification"):**

  1. Add instruction at the top of Step 5: "Classify all files on their full content first. Content budget trimming (if applicable) happens after classification, using the assigned roles to determine priority tiers."

  2. Add a new sub-section **"Step 5b: Content Budget Trimming"** immediately after classification completes:
     - "If `content_budget_active` (total content > 100KB), apply priority-based trimming:"
     - The 9 classification roles map to priority tiers:
       - **Always full read (Tier 1):** PRD / Product Requirements, Technical Specification, Constraints / Non-functional Requirements
       - **Full read if budget allows (Tier 2):** Architecture / System Design, API Specification, Data Model / Schema
       - **Summarize when over budget (Tier 3):** User Stories / Use Cases, UI/UX Specification, Other / General
     - Start with a 100KB budget. Deduct Tier 1 files (always included in full). Then add Tier 2 files in full until budget exhausted. Remaining Tier 2 and all Tier 3 files: produce a condensed summary of each (preserving key requirements, constraints, and any contradictory content — Claude's discretion on summarization approach).
     - Replace the stored full content for summarized files with the summary text. Mark each summarized file with a `summarized: true` flag alongside its filename, role, and content.
     - "If `content_budget_active` is false, skip this sub-section entirely."

  **Step 6 changes (Synthesis / Contradiction Detection):**

  1. In the contradiction detection section, add: "If a file marked `summarized: true` is involved in a detected contradiction or gap, append to the user question: '⚠ Note: {filename} was summarized due to spec folder size. Review the original file for full context before resolving.'"
  2. This implements the CONTEXT.md notification rule — only mention summarization when it's relevant to a user decision. Do NOT list summarized files proactively.

  **Workflow metadata updates:**
  - Add "handles large spec folders (>100KB) via priority-based content budgeting" to the `<purpose>` description at the top of the file
  - Add to the success_criteria checklist at the bottom: "- [ ] Large spec folders handled via content budgeting without token limit failures"

  **Important:** This is a workflow instruction change — the workflow is a markdown prompt executed by an orchestrating agent, not code. Write all budget logic as clear procedural instructions the agent follows, in the same prose style as the existing workflow steps. Do not write JavaScript/Python code blocks for the budget logic.
  </action>
  <verify>
  Read the updated workflow file. Verify:
  1. Step 4 contains edge case handling (empty files, encoding, non-markdown wording)
  2. Step 4 contains content budget check with 100KB threshold
  3. Step 4 has a forward-reference to Step 5b for actual trimming
  4. Step 5 says classification happens on full content before budget trimming
  5. Step 5b defines all 3 priority tiers covering all 9 classification roles
  6. Tier 1 files (PRD, tech spec, constraints) are marked "always full read"
  7. Step 6 has the summarized file notification rule for contradictions
  8. Workflow purpose and success_criteria updated
  9. The workflow reads coherently end-to-end — no broken flow
  </verify>
  <done>
  Workflow has content budget system across Steps 4-6: threshold check in Step 4, priority-based trimming in Step 5b (after classification), notification rule in Step 6. Edge cases (empty files, non-markdown, encoding) handled. Existing behavior unchanged for folders under 100KB.
  </done>
</task>

</tasks>

<verification>
- Read the full workflow from Step 4 through Step 6 to verify the content budget logic flows correctly
- Verify no existing functionality was broken (spec reading, classification, synthesis still work for normal-sized folders)
- Verify the 100KB threshold is clearly stated
- Verify all 9 classification roles are assigned to priority tiers
- Verify the flow: read all → check budget → classify on full content → trim if over budget → synthesize
</verification>

<success_criteria>
- Workflow has a clear 100KB content budget threshold
- Three priority tiers cover all 9 classification roles
- Tier 1 (PRD, tech spec, constraints) always gets full content
- Lower-priority files are summarized when over budget
- User only sees summarization notices when it affects contradiction/gap resolution
- Edge cases (empty files, non-markdown, encoding) handled without aborting
- Existing workflow behavior unchanged for folders under 100KB
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-runtime-polish/04-01-SUMMARY.md`
</output>
