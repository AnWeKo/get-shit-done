---
phase: 02-pipeline-automation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - get-shit-done/workflows/new-project-from-spec.md
autonomous: true

must_haves:
  truths:
    - "Workflow spawns 4 parallel research agents with custom topics derived from spec content, then synthesizes into SUMMARY.md"
    - "Workflow generates REQUIREMENTS.md with domain-specific categories, REQ-IDs, and spec-source traceability"
    - "Research findings inform HOW requirements are written but do not add new requirements beyond what specs define"
    - "Requirements are specific, testable, user-centric statements with proper REQ-ID format"
  artifacts:
    - path: "get-shit-done/workflows/new-project-from-spec.md"
      provides: "Steps 10-11: research execution and requirements generation"
      contains: "gsd-project-researcher"
    - path: "get-shit-done/workflows/new-project-from-spec.md"
      provides: "REQUIREMENTS.md generation with REQ-IDs"
      contains: "REQUIREMENTS.md"
  key_links:
    - from: "Step 10 (Research)"
      to: "4 parallel Task() calls"
      via: "Task(subagent_type='general-purpose', model=researcher_model)"
      pattern: "gsd-project-researcher"
    - from: "Step 10 (Research)"
      to: "research synthesizer"
      via: "Task(subagent_type='gsd-research-synthesizer', model=synthesizer_model)"
      pattern: "gsd-research-synthesizer"
    - from: "Step 11 (Requirements)"
      to: "PROJECT.md specs + research SUMMARY.md"
      via: "reads both to generate informed requirements"
      pattern: "REQUIREMENTS.md"
---

<objective>
Add research execution and requirements generation steps to the spec-from-file workflow.

Purpose: After config is set (Plan 01), the pipeline needs to run research (4 parallel agents analyzing the domain based on spec content) and generate REQUIREMENTS.md with proper REQ-IDs and traceability. This covers PIPE-04 (research always runs), PIPE-05 (requirements generation), and PIPE-09 (traceability).

Output: Workflow extended with Steps 10 (Research) and 11 (Requirements Generation).
</objective>

<execution_context>
@/home/andreas/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/andreas/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pipeline-automation/02-CONTEXT.md
@.planning/phases/02-pipeline-automation/02-01-PLAN.md
@get-shit-done/workflows/new-project-from-spec.md
@get-shit-done/workflows/new-project.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Step 10 — Research Execution</name>
  <files>get-shit-done/workflows/new-project-from-spec.md</files>
  <action>
Add a new Step 10 after Step 9 (Resolve Model Profile). This step runs the full research pipeline — 4 parallel researchers + synthesizer.

**Step 10: Research**

Follow the pattern from `new-project.md` Step 6, but with these spec-from-file adaptations:

**10a. No research decision gate** — research ALWAYS runs in the spec-from-file flow (locked decision). No AskUserQuestion asking whether to research.

**10b. Display simple progress:**
```
Running research (4 parallel researchers)... this may take a minute.
```
(Locked decision: simple waiting message, no per-researcher progress updates.)

**10c. Determine milestone context** (same as new-project.md):
- If no "Validated" requirements in PROJECT.md → Greenfield
- If "Validated" requirements exist → Subsequent milestone

**10d. Derive research topics from spec content:**

Unlike the interactive flow which uses generic questions, the spec-from-file flow derives CUSTOM research topics by analyzing PROJECT.md. The agent should:

1. Read the synthesized PROJECT.md
2. Identify the project's domain, tech stack preferences, and key features
3. Generate domain-specific research questions for each of the 4 dimensions:
   - **Stack**: "What's the standard 2025 stack for [specific domain from specs]?" — include any tech stack constraints from specs
   - **Features**: "What features do [domain] products have?" — informed by what specs already specify
   - **Architecture**: "How are [domain] systems typically structured?" — informed by any architecture hints in specs
   - **Pitfalls**: "What do [domain] projects commonly get wrong?" — informed by the specific approach described in specs

**10e. Spawn 4 parallel researcher agents:**

Use the EXACT same Task() spawn pattern as `new-project.md` Step 6, with the same:
- Agent path: `~/.claude/agents/gsd-project-researcher.md`
- Output files: `.planning/research/STACK.md`, `FEATURES.md`, `ARCHITECTURE.md`, `PITFALLS.md`
- Templates: `~/.claude/get-shit-done/templates/research-project/*.md`
- Model: `{researcher_model}` from init JSON
- subagent_type: `"general-purpose"`
- Quality gates and downstream consumer notes

But customize the `<question>` and `<milestone_context>` blocks with the domain-specific topics derived in step 10d.

Before spawning:
```bash
mkdir -p .planning/research
```

**10f. Spawn synthesizer after all 4 complete:**

Use the EXACT same synthesizer Task() pattern as `new-project.md` Step 6:
- Agent: `gsd-research-synthesizer`
- Model: `{synthesizer_model}`
- Input: the 4 research files
- Output: `.planning/research/SUMMARY.md`
- Template: `~/.claude/get-shit-done/templates/research-project/SUMMARY.md`

**10g. Display completion** (brief, not a full banner):
```
Research complete. Findings in .planning/research/
```

**Do NOT commit** — atomic commit at end (Plan 03).

**Important:** Copy the Task() prompt structures from new-project.md almost verbatim. The only differences are: (1) research questions are derived from spec content instead of being generic, and (2) no decision gate asking whether to research.
  </action>
  <verify>
Read the workflow file and confirm:
1. Step 10 exists titled "Research"
2. No research decision gate (always runs)
3. Simple waiting message displayed
4. 4 parallel Task() calls with correct agent path, output files, templates, model variable
5. Research synthesizer Task() call follows the 4 researchers
6. Research topics are derived from spec content (not hardcoded generic questions)
7. mkdir -p .planning/research is called before spawning
8. No commit in this step
  </verify>
  <done>
Step 10 executes the full research pipeline (4 parallel researchers + synthesizer) with domain-specific topics derived from spec content, matching the interactive flow's Task() patterns exactly — covering PIPE-04.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Step 11 — Requirements Generation</name>
  <files>get-shit-done/workflows/new-project-from-spec.md</files>
  <action>
Add a new Step 11 after Step 10 (Research). This step generates REQUIREMENTS.md from spec content, informed by research findings.

**Step 11: Generate Requirements**

This follows the pattern from `new-project.md` Step 7, but adapted for the spec-from-file flow:

**11a. No interactive scoping** — requirements come from specs, not user questioning. No per-category AskUserQuestion loops. No "Any additions?" question. No approval gate.

**11b. Load context:**
- Read PROJECT.md (synthesized specs from Phase 1)
- Read research/SUMMARY.md (research findings from Step 10)
- Read research/FEATURES.md (feature categories)

**11c. Derive requirements from specs (NOT from research):**

Locked decision from CONTEXT.md: "Research findings inform only — requirements come from specs. Research shapes HOW requirements are written (informed by ecosystem reality), not WHAT they are."

The agent should:
1. Extract all requirements from PROJECT.md's Active requirements list (these came from spec synthesis in Phase 1)
2. Organize into domain-appropriate categories (Claude's discretion — can mirror spec structure or create better groupings)
3. Assign REQ-IDs using the format `[CATEGORY]-[NUMBER]` (e.g., AUTH-01, DATA-02, UI-03)
4. Make each requirement specific, testable, and user-centric:
   - Good: "User can reset password via email link" 
   - Bad: "Handle password reset"
5. Use research findings to INFORM the wording — e.g., if research says "JWT is standard for auth", shape the auth requirement to mention JWT if the spec implied token auth
6. Include traceability: each requirement notes which spec file(s) it originated from (HTML comment pattern from Phase 1: `<!-- from: filename.md -->`)

**11d. Generate REQUIREMENTS.md:**

Write `.planning/REQUIREMENTS.md` using this structure:

```markdown
# Requirements: [Project Name]

**Defined:** [date]
**Core Value:** [from PROJECT.md]

## v1 Requirements

Requirements for initial release. Each maps to roadmap phases.

### [Category 1]

- [ ] **CAT-01**: [Specific, testable requirement] <!-- from: spec-file.md -->
- [ ] **CAT-02**: [Specific, testable requirement] <!-- from: spec-file.md, other.md -->

### [Category 2]

- [ ] **CAT2-01**: [Requirement] <!-- from: spec-file.md -->

## v2 Requirements

Deferred to future release. Tracked but not in current roadmap.

[Any items from specs marked as future/later/v2]

## Out of Scope

Explicitly excluded. Documented to prevent scope creep.

| Feature | Reason |
|---------|--------|
| [From PROJECT.md Out of Scope section] | [Reason] |

## Traceability

Which phases cover which requirements. Updated during roadmap creation.

| Requirement | Phase | Status |
|-------------|-------|--------|
| CAT-01 | TBD | Pending |
| CAT-02 | TBD | Pending |

**Coverage:**
- v1 requirements: [N] total
- Mapped to phases: 0 (roadmap not yet created)
- Unmapped: [N]
```

**11e. Traceability placeholder:**

The Traceability section starts with all requirements unmapped (Phase = TBD, Status = Pending). Step 12 (Roadmap, Plan 03) will update this table with actual phase assignments — covering PIPE-09.

**Do NOT commit** — atomic commit at end (Plan 03).
  </action>
  <verify>
Read the workflow file and confirm:
1. Step 11 exists titled "Generate Requirements"
2. No interactive scoping or approval gates
3. Requirements derived from spec content (PROJECT.md), not from research alone
4. Research findings inform wording but don't add new requirements
5. REQ-ID format is [CATEGORY]-[NUMBER]
6. Domain-specific categories (not hardcoded)
7. Spec-source traceability via HTML comments
8. Traceability section present with TBD phase assignments
9. REQUIREMENTS.md structure matches the existing project's format
10. No commit in this step
  </verify>
  <done>
Step 11 generates REQUIREMENTS.md with domain-specific categories, REQ-IDs, spec-source traceability, and research-informed wording — covering PIPE-05 and setting up PIPE-09 traceability for the roadmap step.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. Workflow file now has Steps 10 (Research) and 11 (Requirements) after Steps 8-9 from Plan 01
2. Research step spawns 4 parallel agents + synthesizer with correct Task() patterns
3. Requirements step generates REQUIREMENTS.md from spec content, informed by research
4. No interactive gates or approval steps (spec-from-file is automated)
5. No commits in either step (atomic commit deferred to Plan 03)
6. Steps 1-9 are unchanged from Plan 01
</verification>

<success_criteria>
- Research always runs (no decision gate) — PIPE-04 addressed
- 4 parallel researcher Task() calls match interactive workflow pattern exactly
- Research topics derived from spec content (not generic)
- Research synthesizer produces SUMMARY.md
- Requirements come from specs, research only informs wording — CONTEXT.md decision honored
- REQUIREMENTS.md has proper REQ-IDs, categories, traceability — PIPE-05, PIPE-09 addressed
- Each requirement is specific, testable, user-centric
- No approval gates anywhere — PIPE-08 partially addressed (commit deferred to Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/02-pipeline-automation/02-02-SUMMARY.md`
</output>
