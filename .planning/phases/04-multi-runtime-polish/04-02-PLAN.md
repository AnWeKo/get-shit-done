---
phase: 04-multi-runtime-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/install.js
  - tests/fixtures/spec-e2e/prd.md
  - tests/fixtures/spec-e2e/tech-spec.md
  - tests/fixtures/spec-e2e/user-stories.md
  - tests/e2e-spec-from-file.test.cjs
autonomous: true

must_haves:
  truths:
    - "The installer correctly converts new-project-from-spec command for Claude Code (nested commands/gsd/), OpenCode (flattened command/gsd-*), and Gemini (TOML)"
    - "A realistic test fixture exists that can exercise the full spec-from-file pipeline"
    - "An automated test verifies the installer produces correct converted output for the new command across all runtimes"
  artifacts:
    - path: "tests/fixtures/spec-e2e/prd.md"
      provides: "Minimal PRD spec for E2E testing"
    - path: "tests/fixtures/spec-e2e/tech-spec.md"
      provides: "Minimal tech spec for E2E testing"
    - path: "tests/fixtures/spec-e2e/user-stories.md"
      provides: "Minimal user stories for E2E testing"
    - path: "tests/e2e-spec-from-file.test.cjs"
      provides: "Automated installer output verification for all 3 runtimes"
  key_links:
    - from: "tests/e2e-spec-from-file.test.cjs"
      to: "bin/install.js"
      via: "Tests invoke installer conversion functions and verify output"
      pattern: "convertClaude|copyFlattenedCommands|copyWithPathReplacement"
---

<objective>
Verify cross-runtime installer output for the new-project-from-spec command and create an E2E test fixture.

Purpose: The installer already handles cross-runtime conversion generically — commands, workflows, and agents are all converted automatically. This plan verifies the conversion produces correct output specifically for the new-project-from-spec command (CMD-03), and creates a purpose-built test fixture that future E2E tests can use to validate the full pipeline.

Output: Test fixture in tests/fixtures/spec-e2e/, automated installer verification test.
</objective>

<execution_context>
@/home/andreas/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/andreas/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-runtime-polish/04-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test fixture spec files</name>
  <files>
    tests/fixtures/spec-e2e/prd.md
    tests/fixtures/spec-e2e/tech-spec.md
    tests/fixtures/spec-e2e/user-stories.md
  </files>
  <action>
  Create a minimal but realistic spec folder in `tests/fixtures/spec-e2e/` that can exercise the full spec-from-file pipeline. Per CONTEXT.md: "Minimal — 2-3 files (PRD and tech spec at minimum), just enough to prove the pipeline produces all 6 artifacts."

  Create 3 files for a simple fictional project (e.g., a task management CLI):

  **prd.md** (~1-2KB):
  - Project name, vision, target users
  - 3-4 core features (enough to generate multiple requirements/phases)
  - Success metrics
  - Include one slightly ambiguous requirement that could trigger the gap detection system

  **tech-spec.md** (~1-2KB):
  - Technology choices (e.g., Node.js, SQLite)
  - Architecture overview (CLI with local storage)
  - Key constraints (e.g., offline-first, no cloud dependencies)
  - Include one mild contradiction with prd.md (e.g., PRD mentions "real-time sync" but tech spec says "offline-first") to exercise contradiction detection

  **user-stories.md** (~0.5-1KB):
  - 3-4 user stories in "As a user, I want..." format
  - Each with acceptance criteria
  - Straightforward content, no contradictions

  The fixture should be:
  - Small enough to run quickly in tests (~3-4KB total)
  - Realistic enough to exercise synthesis, classification, contradiction detection, and gap handling
  - Self-contained (no external references)
  </action>
  <verify>
  ```bash
  ls -la tests/fixtures/spec-e2e/
  wc -c tests/fixtures/spec-e2e/*.md
  ```
  Verify: 3 .md files exist, total size ~3-5KB. Each file has meaningful content (not stubs).
  </verify>
  <done>
  Three spec fixture files exist in tests/fixtures/spec-e2e/ with realistic content including at least one contradiction between files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create automated installer output verification test</name>
  <files>tests/e2e-spec-from-file.test.cjs</files>
  <action>
  Create an automated test that verifies the installer correctly converts the new-project-from-spec command for all 3 runtimes. This addresses CMD-03 and the CONTEXT.md requirement: "Verify the installer produces correct converted files for each runtime (correct paths, tool names, command namespace)."

  Use Node.js built-in test runner (`node:test` + `node:assert`), following the same patterns as existing tests in `tests/*.test.cjs`.

  **Test structure:**

  ```javascript
  // tests/e2e-spec-from-file.test.cjs
  const { describe, test, beforeEach, afterEach } = require('node:test');
  const assert = require('node:assert');
  const fs = require('fs');
  const path = require('path');
  const os = require('os');
  ```

  **Tests to write:**

  1. **"command file exists for source"** — Verify `commands/gsd/new-project-from-spec.md` exists and has valid YAML frontmatter (name field present).

  2. **"workflow file exists for source"** — Verify `get-shit-done/workflows/new-project-from-spec.md` exists and is non-trivial (>500 lines).

  3. **"Claude Code conversion preserves command structure"** — Simulate Claude Code installation: call `copyWithPathReplacement` on the commands directory to a temp dir with `runtime='claude'` and `pathPrefix='~/.claude/'`. Verify the output file at `commands/gsd/new-project-from-spec.md` exists and:
     - Contains the original frontmatter name
     - Has `~/.claude/` path references (not `~/.opencode/`)

  4. **"OpenCode conversion flattens command"** — Simulate OpenCode installation: call `copyFlattenedCommands` on the commands/gsd directory to a temp dir with `runtime='opencode'`. Verify:
     - Output file is named `gsd-new-project-from-spec.md` (flattened)
     - Frontmatter has `tools:` instead of `allowed-tools:`
     - Path references use the OpenCode prefix

  5. **"Gemini conversion produces TOML"** — Simulate Gemini installation: call `copyWithPathReplacement` with `runtime='gemini'` and `isCommand=true`. Verify:
     - Output file is `.toml` (not `.md`)
     - Contains TOML structure (`[command]` section or Gemini equivalent)

  6. **"test fixture spec files exist and are valid"** — Verify the fixture files from Task 1 exist, are non-empty, and contain markdown content.

  **Implementation notes:**
  - The installer functions (`copyFlattenedCommands`, `copyWithPathReplacement`, etc.) are NOT exported — they're local functions in `bin/install.js`. You'll need to either:
    a. Extract them to be importable (adds complexity), OR
    b. Test by running the installer functions end-to-end via a subprocess, OR
    c. Test the RESULT by actually running a mini-install to a temp directory
  
  The cleanest approach: **Run the actual installer** in a temp directory using `node bin/install.js --runtime claude --target /tmp/gsd-test-claude --yes` (check if the installer supports non-interactive mode with flags). If not, test the static source files directly (verify the command and workflow files exist with correct content, since the installer's conversion functions are already tested implicitly by the fact that all other commands work).

  **Practical approach if installer can't be invoked non-interactively:**
  - Test 1-2: Verify source files exist with correct structure
  - Test 3-5: Require the installer's conversion functions by reading install.js and extracting them (complex), OR just verify the source command/workflow are well-formed markdown that the existing installer conversion functions can handle (check for patterns that would break conversion: ensure no raw `${VAR}` in non-bash contexts, ensure frontmatter is valid YAML, ensure `<sub>` tags are used correctly)
  - Test 6: Verify fixture files

  Use whichever approach produces the most meaningful tests with the least complexity. The goal is confidence that CMD-03 is met.
  </action>
  <verify>
  ```bash
  node --test tests/e2e-spec-from-file.test.cjs
  ```
  All tests pass. Also run the full test suite to verify no regressions:
  ```bash
  npm test
  ```
  </verify>
  <done>
  Automated test file exists, all tests pass, and the tests verify that the new-project-from-spec command is correctly structured for cross-runtime installation. Full test suite still passes.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/e2e-spec-from-file.test.cjs` passes
- `npm test` still passes (no regressions)
- Test fixture files are realistic and exercise key pipeline features
- Tests verify source files are well-formed for all 3 runtimes
</verification>

<success_criteria>
- Test fixture exists at tests/fixtures/spec-e2e/ with 3 realistic spec files
- Automated tests verify the command and workflow files are correctly structured for Claude Code, OpenCode, and Gemini conversion
- All new tests pass
- All existing tests still pass (81 passing baseline)
- CMD-03 requirement is addressed through verification
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-runtime-polish/04-02-SUMMARY.md`
</output>
