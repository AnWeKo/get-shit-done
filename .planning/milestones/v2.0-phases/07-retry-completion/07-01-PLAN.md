---
phase: 07-retry-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/plan-phase.md
  - get-shit-done/workflows/plan-all.md
autonomous: true
requirements: [RTRY-01, RTRY-02, RTRY-03, STAT-02]

must_haves:
  truths:
    - "When plan-checker flags issues in batch mode, the planner is re-invoked with checker feedback automatically — no user interaction"
    - "Retries are capped at 3 attempts per phase (matching existing plan-phase Step 12 behavior)"
    - "If retries are exhausted in batch mode, the best available plan is committed and the workflow continues to the next phase"
    - "On batch completion, STATE.md reflects all phases planned with status ready for execution"
  artifacts:
    - path: "get-shit-done/workflows/plan-phase.md"
      provides: "Batch-mode retry handling — auto-force-proceed on exhaustion"
      contains: "--batch"
    - path: "get-shit-done/workflows/plan-all.md"
      provides: "Batch flag passed to plan-phase, final STATE.md update for ready-for-execution"
      contains: "--batch"
  key_links:
    - from: "get-shit-done/workflows/plan-all.md"
      to: "get-shit-done/workflows/plan-phase.md"
      via: "--batch flag in Task subagent spawn"
      pattern: "--batch"
    - from: "get-shit-done/workflows/plan-phase.md"
      to: "plan-phase Step 12 retry logic"
      via: "batch mode auto-force-proceed when iteration_count >= 3"
      pattern: "batch.*force.proceed|auto.*proceed"
    - from: "get-shit-done/workflows/plan-all.md"
      to: "gsd-tools.cjs state patch"
      via: "Final STATE.md update with ready-for-execution status"
      pattern: "state patch.*ready"
---

<objective>
Add batch-mode retry handling to plan-phase.md and wire it through plan-all.md so that plan-checker failures are automatically retried (up to 3 times), retry exhaustion auto-proceeds with best plan, and batch completion leaves STATE.md in a clean "ready for execution" state.

Purpose: The final piece of `/gsd-plan-all` — ensures the batch workflow is fully autonomous. Without this, checker failures in batch mode would cause the plan-phase subagent to hang waiting for user input that never comes.
Output: Updated `plan-phase.md` with `--batch` flag handling, updated `plan-all.md` with `--batch` passthrough and final state update.
</objective>

<execution_context>
@/home/andreas/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/andreas/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-sequential-planning-loop/06-01-SUMMARY.md
@get-shit-done/workflows/plan-phase.md
@get-shit-done/workflows/plan-all.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --batch flag to plan-phase.md for autonomous retry handling</name>
  <files>get-shit-done/workflows/plan-phase.md</files>
  <action>
Modify `get-shit-done/workflows/plan-phase.md` to support a `--batch` flag that makes the retry loop fully autonomous. Three changes are needed:

**Change 1 — Step 2 (Parse and Normalize Arguments):**
Add `--batch` to the list of recognized flags. The line currently reads:
```
Extract from $ARGUMENTS: phase number (integer or decimal like `2.1`), flags (`--research`, `--skip-research`, `--gaps`, `--skip-verify`).
```
Change it to include `--batch`:
```
Extract from $ARGUMENTS: phase number (integer or decimal like `2.1`), flags (`--research`, `--skip-research`, `--gaps`, `--skip-verify`, `--batch`).
```

**Change 2 — Step 4 (Load CONTEXT.md):**
When `--batch` flag is set AND `context_path` is null, automatically "Continue without context" — do NOT present the user question. Add this right after the `context_path` null check:

```
**If `--batch` flag is set:** Always "Continue without context" — do not prompt.
```

This goes before the existing "Use question:" block, so when --batch is set, it skips the interactive prompt entirely.

**Change 3 — Step 12 (Revision Loop):**
The current behavior when `iteration_count >= 3` is:
```
Display: `Max iterations reached. {N} issues remain:` + issue list
Offer: 1) Force proceed, 2) Provide guidance and retry, 3) Abandon
```

Add a batch-mode override BEFORE this offer block:
```
**If `--batch` flag is set:**
Display: `Max iterations reached (batch mode). Proceeding with best available plan. {N} issues remain.`
Proceed directly to step 13 (skip the interactive offer).
```

The existing interactive behavior (Offer: 1/2/3) remains as the else case for when `--batch` is NOT set.

**Important:** Do NOT modify the retry loop itself (Steps 10-11). The existing plan→check→revise cycle already works correctly — plan-phase already re-invokes the planner with checker feedback (RTRY-01) and caps at 3 iterations (RTRY-02). The only missing piece is the batch-mode auto-proceed behavior (RTRY-03).

**Also update Step 6 (Check Existing Plans):**
When `--batch` flag is set AND existing plans are found, automatically proceed to step 7 without offering the interactive choice (1. Add more plans, 2. View existing, 3. Replan from scratch). In batch mode, existing plans mean the phase was already planned — skip it by returning "PLANNING COMPLETE" with a note that plans already existed.
  </action>
  <verify>
    <automated>grep -c "\-\-batch" get-shit-done/workflows/plan-phase.md | grep -qv "^0$" && grep "batch.*mode" get-shit-done/workflows/plan-phase.md | grep -qi "proceed\|force\|auto" && echo "PASS" || echo "FAIL"</automated>
    <manual>Read plan-phase.md Steps 2, 4, 6, and 12. Verify: --batch is listed as a recognized flag; Step 4 skips context prompt in batch mode; Step 6 skips interactive choice in batch mode; Step 12 has a batch-mode auto-proceed path before the interactive offer.</manual>
  </verify>
  <done>
plan-phase.md recognizes --batch flag. In batch mode: Step 4 skips context prompt, Step 6 skips "existing plans" choice (returns PLANNING COMPLETE), Step 12 auto-proceeds with best plan on retry exhaustion instead of asking the user.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire --batch flag in plan-all.md and add STAT-02 completion state</name>
  <files>get-shit-done/workflows/plan-all.md</files>
  <action>
Modify `get-shit-done/workflows/plan-all.md` with two changes:

**Change 1 — Step 5.3 (Spawn plan-phase Subagent):**
The current flags building logic is:
```
Build flags string from init config:
- If `research_enabled` is false: add `--skip-research`
- If `plan_checker_enabled` is false: add `--skip-verify`
```

Add `--batch` to the flags that are always passed in batch mode:
```
Build flags string from init config:
- Always add: `--batch` (enables autonomous retry handling)
- If `research_enabled` is false: add `--skip-research`
- If `plan_checker_enabled` is false: add `--skip-verify`
```

Also update the Task spawn prompt in Step 5.3. The current `<arguments>` section reads:
```
ARGUMENTS='{PHASE_NUM} {flags}'
```
This already works — `--batch` will be in `{flags}`. No change needed to the template itself.

Also add an instruction line to the `<instructions>` block inside the Task prompt:
```
6. The --batch flag ensures autonomous operation — plan-phase will auto-proceed on retry exhaustion without user interaction
```

**Change 2 — Step 5.7 (Completion Summary):**
After the existing STATE.md update (`state patch --"Status" "Batch planning complete" ...`), add a more specific update that satisfies STAT-02 (STATE.md reflects all phases planned, ready for execution):

Change the existing state patch from:
```bash
node ~/.claude/get-shit-done/bin/gsd-tools.cjs state patch \
  --"Status" "Batch planning complete" \
  --"Last activity" "{date} — Batch planned {SUCCESS_COUNT} phases"
```

To a more detailed update that makes STATE.md reflect readiness:
```bash
node ~/.claude/get-shit-done/bin/gsd-tools.cjs state patch \
  --"Status" "All phases planned — ready for execution" \
  --"Last activity" "{date} — Batch planned {SUCCESS_COUNT}/{TOTAL_TO_PLAN} phases" \
  --"Current focus" "Ready for /gsd-execute-phase"
```

If `FAILURE_COUNT > 0`, use a different status:
```bash
node ~/.claude/get-shit-done/bin/gsd-tools.cjs state patch \
  --"Status" "Batch planning complete ({FAILURE_COUNT} failed)" \
  --"Last activity" "{date} — Batch planned {SUCCESS_COUNT}/{TOTAL_TO_PLAN} phases ({FAILURE_COUNT} failed)" \
  --"Current focus" "Review failed phases"
```

**Important:** Keep all existing plan-all.md content intact. Only modify the flags logic in Step 5.3 and the STATE.md update in Step 5.7. Do not change Steps 1-4, Step 5.1-5.2, Step 5.4-5.6, or the offer_next/success_criteria sections.

Also add a new success criterion to the `<success_criteria>` section:
```
- [ ] --batch flag is passed to plan-phase subagent for autonomous retry handling
- [ ] Final STATE.md update reflects "ready for execution" when all phases succeed
```
  </action>
  <verify>
    <automated>grep -c "\-\-batch" get-shit-done/workflows/plan-all.md | grep -qv "^0$" && grep "ready for execution" get-shit-done/workflows/plan-all.md | grep -qi "state\|status" && echo "PASS" || echo "FAIL"</automated>
    <manual>Read plan-all.md Step 5.3 and Step 5.7. Verify: --batch is in the flags list, the Task prompt mentions batch autonomy, the completion STATE.md update uses "ready for execution" (success case) or includes failure count (failure case).</manual>
  </verify>
  <done>
plan-all.md passes --batch flag to every plan-phase invocation. Final STATE.md update reflects "All phases planned — ready for execution" on success, or "Batch planning complete (N failed)" on partial failure. Success criteria updated.
  </done>
</task>

</tasks>

<verification>
1. `grep "\-\-batch" get-shit-done/workflows/plan-phase.md` — plan-phase recognizes --batch flag
2. `grep "\-\-batch" get-shit-done/workflows/plan-all.md` — plan-all passes --batch flag
3. `grep "batch.*mode" get-shit-done/workflows/plan-phase.md` — batch mode auto-proceed logic exists
4. `grep "ready for execution" get-shit-done/workflows/plan-all.md` — STAT-02 final state update
5. `grep -c "iteration_count.*3" get-shit-done/workflows/plan-phase.md` — retry cap still at 3 (RTRY-02 preserved)
6. Verify no interactive prompts in batch path: Step 4 context prompt skipped, Step 6 existing plans skipped, Step 12 offer skipped
</verification>

<success_criteria>
- plan-phase.md recognizes --batch flag in Step 2
- plan-phase.md Step 4 auto-continues without context in batch mode
- plan-phase.md Step 6 returns PLANNING COMPLETE if plans exist in batch mode
- plan-phase.md Step 12 auto-force-proceeds in batch mode when retries exhausted
- plan-all.md Step 5.3 always adds --batch to flags
- plan-all.md Step 5.7 updates STATE.md to "ready for execution" on success
- plan-all.md Step 5.7 updates STATE.md with failure count when some phases fail
- Existing retry loop (Steps 10-11) unchanged — still re-invokes planner with checker feedback (RTRY-01)
- Retry cap stays at 3 iterations (RTRY-02)
- No interactive prompts are reachable in batch mode
</success_criteria>

<output>
After completion, create `.planning/phases/07-retry-completion/07-01-SUMMARY.md`
</output>
